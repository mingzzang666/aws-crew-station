bannerService.showList(bannerLayout.list);

const registerBtn = document.querySelector("add-btn");
const addBtn = document.querySelector("banner-file");

registerBtn.addEventListener("click", (e) =>{
    console("사진 등록");
});

